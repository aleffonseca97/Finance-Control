# Copy to .env.test and point at a dedicated Postgres database.
# Never use production credentials here.

DATABASE_URL="postgresql://6V8VkZh5oOxgJLeJ:logosserveradmin193690@72.62.108.253:32772/financemaimo_dev?schema=public"

# Same secrets as local app are fine for tests; override if needed.
NEXTAUTH_URL="http://127.0.0.1:3000"
NEXTAUTH_SECRET="test-secret-change-me"

# Optional: Playwright against an already running server
# PLAYWRIGHT_BASE_URL="http://127.0.0.1:3000"
